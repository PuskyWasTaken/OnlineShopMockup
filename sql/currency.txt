-- DROP TABLE currency;

CREATE TABLE currency
(
    id bigint NOT NULL,
    currency_key character varying(255) COLLATE pg_catalog."default" NOT NULL,
    base_exchange_rate numeric(21,2) NOT NULL,
    CONSTRAINT currency_pkey PRIMARY KEY (id),
    CONSTRAINT ux_currency_currency_key UNIQUE (currency_key)
)